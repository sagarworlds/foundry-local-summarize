<UserControl x:Class="FoundrySummarizer.Wpf.Views.IngestionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header / Description -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Multi-Format Ingestion Pipeline" Style="{StaticResource HeadingLarge}" />
            <TextBlock Text="Parse and chunk native documents (.docx, .pptx, .pdf, .txt, .mp3, .wav) before summarization." Style="{StaticResource BodyText}" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Drop Zone & Action Bar -->
        <Border Grid.Row="1" Style="{StaticResource ModernCard}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="Selected Document File:" Style="{StaticResource MutedText}" />
                    <TextBlock Text="{Binding SelectedFilePath, FallbackValue='No file selected (using default sample)'}" 
                               Foreground="{StaticResource TextPrimaryBrush}" FontSize="14" FontWeight="Medium" Margin="0,4,0,4"
                               TextTrimming="CharacterEllipsis" />
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource AccentPrimaryBrush}" FontSize="12" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="📂 Browse Document..." Style="{StaticResource SecondaryButton}" Command="{Binding BrowseFileCommand}" Margin="0,0,8,0" />
                    <!-- Next step after ingestion: opens the Persona Summarizer and generates a summary of this document. -->
                    <Button Content="✨ Summarize This Document" Style="{StaticResource PrimaryButton}" Command="{Binding SummarizeDocumentCommand}"
                            ToolTip="Open the Persona Summarizer and generate a summary of the ingested document" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Workspace (Extracted Text + Chunks) -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <!-- Extracted Raw Text -->
            <Border Grid.Column="0" Style="{StaticResource ModernCard}" Margin="0,0,12,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="Extracted Document Content" Style="{StaticResource HeadingMedium}" DockPanel.Dock="Left" />
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                            <TextBlock Text="{Binding CharacterCount, StringFormat='Characters: {0:N0}'}" Style="{StaticResource MutedText}" Margin="0,0,12,0" />
                            <TextBlock Text="{Binding EstimatedTokens, StringFormat='Tokens: ~{0:N0}'}" Style="{StaticResource MutedText}" />
                        </StackPanel>
                    </DockPanel>

                    <TextBox Grid.Row="1" Text="{Binding ExtractedText, Mode=TwoWay}"
                             Style="{StaticResource ModernTextBox}"
                             AcceptsReturn="True" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto" />
                </Grid>
            </Border>

            <!-- Chunks Inspector -->
            <Border Grid.Column="1" Style="{StaticResource ModernCard}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="Semantic Chunks Visualizer" Style="{StaticResource HeadingMedium}" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding Chunks.Count, StringFormat='Total Chunks: {0}'}" Style="{StaticResource MutedText}" DockPanel.Dock="Right" HorizontalAlignment="Right" />
                    </DockPanel>

                    <ListBox Grid.Row="1" ItemsSource="{Binding Chunks}"
                             Background="Transparent" BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource SurfaceCard}" Margin="0,0,0,8">
                                    <StackPanel>
                                        <DockPanel>
                                            <TextBlock Text="{Binding Id}" FontWeight="SemiBold" Foreground="{StaticResource AccentPrimaryBrush}" FontSize="12" DockPanel.Dock="Left" />
                                            <TextBlock Text="{Binding TokenCount, StringFormat='~{0} tokens'}" Style="{StaticResource MutedText}" DockPanel.Dock="Right" HorizontalAlignment="Right" />
                                        </DockPanel>
                                        <TextBlock Text="{Binding Text}" Style="{StaticResource BodyText}" Margin="0,6,0,0" TextWrapping="Wrap" MaxHeight="80" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
